version: '3.8'

services:
  imgcap-app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    volumes:
      - ./models:/app/models  # Optional: for caching downloaded models
    restart: unless-stopped